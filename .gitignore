# Rust build output
/target

# Tauri generated schemas (regenerated on build)
/apps/draftos-desktop/gen/schemas/

# OS / editor cruft
.DS_Store
*.swp
*.rs.bk

# LibreOffice/Word lock files
.~lock.*#

# Generated drafts saved into the repo root (real precedents live outside the repo)
/*.docx
/*.md.docx

#cargo run --release -p draftos-desktop